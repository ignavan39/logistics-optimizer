{
  "name": "@logistics/proto",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'proto is .proto files, no build needed'",
    "typecheck": "echo 'proto is .proto files, no typecheck needed' && exit 0"
  },
  "dependencies": {
    "protobufjs": "^7.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}